# Gradle Daemon Toolchain — specifies the JVM the Gradle daemon itself runs on.
# Gradle 8.8+ will auto-detect a matching local JDK or download one if absent.
# This ensures CLI and IDE share the same daemon, avoiding multiple daemon spawns.
toolchainVersion=17
toolchainVendor=ADOPTIUM
